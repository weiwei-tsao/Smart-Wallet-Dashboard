基于你当前的情况（v1.0 对应 main），采用以下版本管理策略：

## 🎯 **V2.0 版本管理规划**

### **1. 当前状态分析**

- `main` 分支 = V1.0 版本（当前功能）
- 需要开发 V2.0 版本
- 需要保持 V1.0 的稳定性

### **2. 推荐的分支策略**

#### **方案 A：主分支分离策略（推荐）**

```
main (V1.0 生产环境)
├── v1.0 (V1.0 维护分支)
├── develop (V2.0 开发主分支)
├── feature/* (V2.0 功能分支)
└── release/* (V2.0 发布分支)
```

#### **具体操作步骤：**

```bash
# 1. 备份当前 V1.0 到专门分支
git checkout main
git checkout -b v1.0
git push origin v1.0

# 2. 创建 V2.0 开发分支
git checkout main
git checkout -b develop
git push origin develop

# 3. 在 main 分支打 V1.0 标签
git checkout main
git tag -a v1.0.0 -m "Release version 1.0.0 - Current stable version"
git push origin v1.0.0

# 4. 开始 V2.0 开发
git checkout develop
git checkout -b feature/nextjs-migration
```

### **3. 版本管理策略**

#### **版本号规划：**

- **V1.0.x** - V1.0 的 bug 修复（在 v1.0 分支）
- **V2.0.x** - V2.0 的新功能开发（在 develop 分支）
- **V2.1.x** - V2.0 的后续迭代

#### **分支职责：**

| 分支        | 用途            | 版本   | 状态   |
| ----------- | --------------- | ------ | ------ |
| `main`      | V1.0 生产环境   | V1.0.x | 稳定   |
| `v1.0`      | V1.0 维护分支   | V1.0.x | 维护   |
| `develop`   | V2.0 开发主分支 | V2.0.x | 开发中 |
| `feature/*` | V2.0 功能开发   | V2.0.x | 开发中 |

### **4. 具体实施计划**

#### **第一阶段：分支准备**

```bash
# 1. 备份当前版本
git checkout main
git checkout -b v1.0
git push origin v1.0

# 2. 创建 V2.0 开发环境
git checkout main
git checkout -b develop
git push origin develop

# 3. 打标签
git checkout main
git tag -a v1.0.0 -m "V1.0 - Current stable version"
git push origin v1.0.0
```

#### **第二阶段：V2.0 开发**

```bash
# 1. 开始第一个功能
git checkout develop
git checkout -b feature/nextjs-migration

# 2. 开发完成后合并
git checkout develop
git merge feature/nextjs-migration
git push origin develop

# 3. 继续下一个功能
git checkout -b feature/backend-bff
```

#### **第三阶段：V2.0 发布**

```bash
# 1. 创建发布分支
git checkout develop
git checkout -b release/v2.0.0

# 2. 测试和修复
# ... 测试和修复 ...

# 3. 发布到生产
git checkout main
git merge release/v2.0.0
git tag -a v2.0.0 -m "V2.0 - Next.js + BFF + Docker"
git push origin main --tags

# 4. 合并回开发分支
git checkout develop
git merge release/v2.0.0
```

### **5. 版本维护策略**

#### **V1.0 维护（如果需要）：**

```bash
# 在 v1.0 分支修复 bug
git checkout v1.0
git checkout -b hotfix/v1.0.1
# ... 修复 bug ...
git checkout v1.0
git merge hotfix/v1.0.1
git tag -a v1.0.1 -m "V1.0.1 - Bug fixes"
git push origin v1.0 --tags

# 同步到 main
git checkout main
git merge v1.0
git push origin main
```

#### **V2.0 开发：**

```bash
# 所有 V2.0 功能都在 develop 分支开发
git checkout develop
git checkout -b feature/new-feature
# ... 开发 ...
git checkout develop
git merge feature/new-feature
```

### **6. 分支保护设置**

#### **GitHub 仓库设置：**

**main 分支（V1.0 生产）：**

- ✅ Require pull request reviews
- ✅ Require status checks to pass
- ✅ Restrict pushes that create files
- ✅ Include administrators

**develop 分支（V2.0 开发）：**

- ✅ Require pull request reviews
- ✅ Require status checks to pass

**v1.0 分支（V1.0 维护）：**

- ✅ Require pull request reviews
- ✅ Require status checks to pass

### **7. 提交信息规范**

```bash
# V1.0 维护
git commit -m "fix(v1.0): 修复钱包连接问题"
git commit -m "hotfix: 紧急修复安全漏洞"

# V2.0 开发
git commit -m "feat(v2.0): 实现 Next.js 迁移"
git commit -m "feat(v2.0): 添加后端 BFF 服务"
git commit -m "feat(v2.0): 集成 Docker 容器化"
```

### **8. 标签管理**

```bash
# V1.0 标签
git tag -a v1.0.0 -m "V1.0 - Current stable version"
git tag -a v1.0.1 -m "V1.0.1 - Bug fixes"

# V2.0 标签
git tag -a v2.0.0 -m "V2.0 - Next.js + BFF + Docker"
git tag -a v2.0.1 -m "V2.0.1 - Performance improvements"

# 里程碑标签
git tag -a milestone/v2.0-frontend -m "V2.0 Frontend migration completed"
git tag -a milestone/v2.0-backend -m "V2.0 Backend BFF completed"
```

### **9. 实际操作建议**

#### **立即执行：**

```bash
# 1. 备份当前版本
git checkout main
git checkout -b v1.0
git push origin v1.0

# 2. 创建 V2.0 开发分支
git checkout main
git checkout -b develop
git push origin develop

# 3. 打 V1.0 标签
git checkout main
git tag -a v1.0.0 -m "V1.0 - Current stable version"
git push origin v1.0.0

# 4. 开始 V2.0 开发
git checkout develop
git checkout -b feature/nextjs-migration
```

### **10. 优势分析**

✅ **版本隔离**：V1.0 和 V2.0 完全分离，互不影响
✅ **稳定维护**：V1.0 可以继续维护和修复
✅ **并行开发**：V2.0 可以独立开发新功能
✅ **清晰历史**：每个版本都有清晰的提交历史
✅ **灵活发布**：可以独立发布 V1.0 和 V2.0 的更新
✅ **风险控制**：V2.0 开发不影响 V1.0 的稳定性

这样的版本管理策略既保护了现有的 V1.0 功能，又为 V2.0 的开发提供了清晰的结构
